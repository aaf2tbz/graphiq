[package]
name = "graphiq-cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "graphiq"
path = "src/main.rs"

[dependencies]
graphiq-core = { path = "../graphiq-core" }
clap = { version = "4", features = ["derive"] }

[features]
default = []
embed = ["graphiq-core/embed"]
